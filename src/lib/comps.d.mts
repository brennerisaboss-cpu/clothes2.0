/** The `wanted`/`w2`/`visible` CTEs deciding which items lend comps to which. */
export const VISIBLE_ITEMS_CTE: string;
/** The `where` clause keeping one comp per piece rather than one per snapshot. */
export const HEAD_OF_CHAIN: string;
